<%= notice %>
<div class="container">
  <div class="row">
    <div class="col-md-3">
      <h2>User info</h2>
</div>
    <%= image_tag current_user.get_profile_image(100,100) %>
  <p>name：<%= @user.name %></p>
  <p>introduction：<%= @user.introduction %></p>
   <p><%= link_to "edit", edit_user_path(@user.id) %></p>





<h1>New book</h1>
<% if @books.errors.any? %>
  <%= @books.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @books.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
<%= form_with model: @books do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Opinion</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Create Book' %>
<% end %>



<h1>Book detail</h1>
 <%= image_tag current_user.get_profile_image(100,100) %>
 <p><%= link_to @user.name, user_path(@user.id) %></p>
<p>
    <strong>Title:</strong>
    <%= link_to @book.title, book_path(@book.id) %>
</p>
<p>
     <strong>show:</strong>
     <%= @book.body %>
</p>

 <%= link_to "Edit", edit_book_path(@book.id) %>
 <% if @book.user == current_user %>
 <%= link_to "Destroy", book_path(@book), method: :delete, "data-confirm" => "本当に消しても大丈夫ですか？" %>

